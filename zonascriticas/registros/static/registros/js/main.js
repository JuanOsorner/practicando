import { RegistrosController } from './RegistrosController.js';

document.addEventListener('DOMContentLoaded', () => {
    // Registramos la licencia de Syncfusion si la tienes (opcional por ahora en demo)
    // ejs.base.registerLicense('TU_LICENCIA_AQUI'); 
    
    new RegistrosController();
});